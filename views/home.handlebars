<main>
    <h2>Home</h2>
    {{#each books}}
    <div class="book">
        <h3>Titulo: {{this.title}}</h3>
        <p>Quantidade de páginas: {{this.pageqty}}</p>
        <a href="/book/ {{this.id}}">ver mais</a>
    </div>
    {{/each}}
</main>

